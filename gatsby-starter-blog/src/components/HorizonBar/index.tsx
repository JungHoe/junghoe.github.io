import styled from 'styled-components';

const HorizonBar = styled.div`
  width: 100%; /* 바의 가로 길이를 100%로 설정하여 수평으로 꽉 차도록 합니다. */
  height: 2px; /* 바의 높이를 설정합니다. */
  background-color: #dddddd; /* 바의 배경 색상을 지정합니다. */
  margin: auto;
`;

export default HorizonBar;
